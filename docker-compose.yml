# docker compose up

version: "3.1"

services:
  app:
    build: .
    container_name: ottai-nightscout-uploader
    restart: always
    env_file:
      - .env
    environment:
      PYTHONUNBUFFERED: 1
    command: python -u main.py
